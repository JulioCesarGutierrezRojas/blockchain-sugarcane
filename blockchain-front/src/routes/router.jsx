import { BrowserRouter, Routes, Route } from "react-router";
import { Navigate } from "react-router";

const AppRouter = ()=>{
    return(
        <BrowserRouter>
            <Routes>
                <Route></Route>
            </Routes>
        </BrowserRouter>
    );
}